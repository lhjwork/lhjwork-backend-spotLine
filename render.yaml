services:
  - type: web
    name: spotline-backend
    env: node
    plan: free
    buildCommand: pnpm run render:build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
